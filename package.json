{
  "name": "js-ipl-data-project",
  "version": "1.0.0",
  "description": "To transform raw data of IPL to calculate the stats",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write \"**/*.js\""
  },
  "author": "Chandu_Malla",
  "license": "ISC",
  "dependencies": {
    "csv-parser": "^3.0.0"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
